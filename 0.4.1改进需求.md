# 豆瓣订阅助手 0.4.1 后续改进需求

本文档记录 0.4.0 发布后确认的问题和下一阶段需求。当前只记录设计，不代表以下功能已经实现。

## 1. 已确认的当前行为

### 1.1 MoviePilot 订阅历史尚未参与查重

当前创建订阅前只调用：

```python
SubscribeOper().get_by(
    type=MediaType.TV.value,
    tmdbid=tmdb_id,
    season=season,
)
```

它只能发现当前订阅表中的相同电视剧和季度，不会检查 MoviePilot 的“电视剧订阅历史”。

下一版应在候选已经确定、创建订阅之前增加：

```python
SubscribeOper().exist_history(
    tmdbid=tmdb_id,
    season=season,
)
```

相同 TMDB ID 且相同季度已经存在于订阅历史时：

- 不再创建订阅。
- RSS 记录状态使用独立状态，例如 `history_existing`。
- 原因显示“MoviePilot 电视剧订阅历史中已存在相同 TMDB 与季度”。
- 将该 RSS 键写入永久成功索引，避免后续周期反复处理。
- 不修改或删除 MoviePilot 原订阅历史。

### 1.2 `no_candidate` 目前缺少诊断信息

当前 `no_candidate` 只能说明没有形成任何可评分候选，无法区分：

- MoviePilot 调用 TMDB 时发生异常。
- 所有搜索词均返回空列表。
- 搜索结果缺少 TMDB ID。
- 找到了 TMDB ID，但 `recognize_media()` 加载详情失败。
- 豆瓣没有提供可用的原标题或别名。

`小丑回魂：欢迎来到德里镇 第一季` 在 TMDB 中实际存在，对应 `IT: Welcome to Derry`、TMDB ID `200875`。该案例说明候选检索、错误诊断和重试能力需要增强，而不是简单降低匹配分数。

## 2. RSS 处理记录页面

### 2.1 搜索输入框

现有 `/history/search` API 已支持 `keyword`、`status`、`category`、`offset` 和 `limit`，但插件页面没有输入框，因此用户无法在界面中使用该能力。

页面需要增加：

- 关键词输入框。
- 搜索按钮。
- 清除按钮。
- 搜索结果总数。
- 分页和每页数量。
- 输入标题、豆瓣 ID、TMDB ID、状态、来源地址或原因均可匹配。

优先使用自定义 Vue 插件页面实现搜索、筛选、分页和行内操作。MoviePilot 的静态 JSON 页面可以绑定固定按钮到插件 API，但没有找到成熟的“输入框值作为 API 动态参数并刷新表格”的官方用例；不应为了少写前端代码牺牲搜索体验。

### 2.2 失败筛选

页面增加状态分段筛选：

- 全部
- 失败
- 成功
- 已跳过

“失败”至少包含：

```text
feed_error
douban_not_found
douban_total_missing
no_candidate
low_score
ambiguous
tmdb_detail_missing
subscribe_failed
lock_failed
error
```

成功状态：

```text
subscribed
existing
history_existing
```

已跳过状态：

```text
category_skipped
```

状态分组应由后端常量统一维护，API 和页面不得分别硬编码两套名单。

### 2.3 单条重试按钮

每条失败记录后增加“重试”按钮。成功和已跳过记录默认不显示该按钮。

建议新增接口：

```text
POST /history/retry
body: {"key": "RSS 条目永久键"}
```

重试流程：

1. 通过 `key` 从完整历史中精确读取记录。
2. 使用历史中保存的标题、链接、GUID、描述、发布时间、来源 RSS、豆瓣 ID、年份和海报重建 `FeedItem`。
3. 获取与 RSS 同步共用的互斥锁，防止单条重试和定时 RSS 同时修改历史及永久索引。
4. 只运行这一条的 `_process_item()`，不能重新扫描整个 RSS。
5. 使用相同 `key` 覆盖旧历史记录，不追加重复行。
6. 成功后写入 `processed_items`；失败时保留最新失败状态和诊断信息。
7. 接口返回最新记录，页面刷新该行。

按钮交互要求：

- 点击后立即进入加载和禁用状态，避免重复提交。
- 任务运行时显示“正在重试”。
- 完成后显示最新状态、原因、TMDB ID、季度和分数。
- RSS 全量同步正在运行时返回明确的“任务忙”提示。
- 重试可能创建真实订阅，按钮提示文字必须明确，不做静默后台操作。

## 3. TMDB 候选检索诊断和重试

### 3.1 保存每个搜索路径

每条处理历史增加 `search_attempts`，每次搜索至少保存：

```json
{
  "query": "IT: Welcome to Derry",
  "season": 1,
  "mode": "base_and_season",
  "result_count": 1,
  "hydrated_count": 1,
  "error": ""
}
```

历史页面默认只显示简短原因，展开详情时再展示搜索路径，避免表格过宽。

### 3.2 细分失败状态

建议将现有笼统的 `no_candidate` 细分为：

- `tmdb_search_error`：TMDB 搜索调用异常。
- `tmdb_search_empty`：所有搜索路径均正常返回，但结果都是空。
- `tmdb_detail_missing`：取得 TMDB ID，但详情加载全部失败。
- `no_candidate`：搜索和详情均正常，但没有产生结构有效的候选。

### 3.3 请求重试

TMDB 搜索或详情加载发生网络异常、限流或服务错误时：

- 单条搜索最多重试 2 次。
- 使用短暂递增退避，例如 2 秒、5 秒。
- 空搜索结果不立即当作网络错误重试，先继续其他标题路径。
- 一条记录只要有任一路径成功得到候选，就继续评分。
- 不降低 `MATCH_ACCEPT_SCORE` 和 `MATCH_MIN_LEAD`。

## 4. “XXXX篇”分季匹配

### 4.1 目标案例

```text
豆瓣标题：鬼灭之刃 无限列车篇
豆瓣总集数：7
主剧：鬼灭之刃
目标：在 TMDB 主剧的所有季度中，通过季度名称匹配“无限列车篇”，再结合年份和 7 集结构确认季度。
```

当前逻辑会把没有明确“第 N 季”的标题当作独立作品搜索，并默认候选季度为第 1 季。因此 TMDB 只把该篇章建成主剧的一个季度时，可能得到 `no_candidate`。

### 4.2 篇章标题假设

增加新的 `arc_title` 假设，拆分：

```text
鬼灭之刃 无限列车篇
          ↓
base_title = 鬼灭之刃
arc_title  = 无限列车篇
```

只在存在清晰分隔符时自动拆分，例如空格、冒号、短横线或间隔点。不要把所有以“篇”结尾的原生作品名强制拆开。

初始安全模式可接受：

```regex
^(?P<base>.+?)[\s:：·\-]+(?P<arc>[^:：·\-]{2,20}篇)$
```

原始完整标题仍必须保留为独立作品候选，篇章路径只是新增候选，不能覆盖原路径。

### 4.3 获取并匹配 TMDB 分季名称

处理 `arc_title` 假设时：

1. 使用 `base_title` 搜索 TMDB 主剧。
2. 对每个主剧候选调用：

   ```python
   TmdbChain().tmdb_seasons(tmdbid)
   ```

3. 读取每季的：
   - `name`
   - `season_number`
   - `episode_count`
   - `air_date`
4. 标准化比较 `arc_title` 与季度 `name`。
5. 为匹配到的季度生成 `(tmdb_id, season_number)` 候选。
6. 再使用豆瓣年份、总集数、演员和主创信息进入统一评分。

不得因为“XXXX篇”直接猜测季号。季号必须来自 TMDB 返回的 `season_number`。

### 4.4 篇章候选证据

建议新增结构证据：

- 篇章名与 TMDB 季名完全一致：强证据。
- 去掉“篇”“Arc”等通用后缀后一致：强证据。
- 篇章名与季名互相包含：中等证据。
- 豆瓣总集数与 TMDB `episode_count` 一致：强辅助证据。
- 豆瓣年份与 TMDB 季 `air_date` 年份一致：强辅助证据。
- 主剧标题与 TMDB 主剧标题或别名一致：必要身份基础。

仅有“篇”字或仅有集数一致不能自动接受。

### 4.5 多语言季名

TMDB 按 MoviePilot 当前语言返回的季名可能是：

```text
无限列车篇
Mugen Train Arc
Season 2
```

实现时应：

- 先比较当前语言季名。
- 使用豆瓣原标题、别名中的篇章文本生成额外比较词。
- 对中文“篇”和英文 `Arc` 做后缀归一化。
- 若 TMDB 只返回“Season N”这类通用名，不把它当作篇章名匹配成功。
- 多语言名称仍无法建立证据时保持拒绝，不按集数单独猜季。

## 5. 必须增加的测试

### 5.1 订阅历史

- 相同 TMDB、相同季度在 MoviePilot 历史中存在时跳过。
- 相同 TMDB、不同季度不能跳过。
- `history_existing` 写入永久成功索引。

### 5.2 页面和重试

- 失败分组包含全部失败状态，不包含 `category_skipped`。
- 搜索 API 可按标题、豆瓣 ID、TMDB ID 和原因查询。
- 只有失败记录显示重试操作。
- 单条重试覆盖旧记录，不产生重复历史。
- 重试成功后写入永久索引。
- RSS 同步占用锁时拒绝单条重试。

### 5.3 搜索诊断

- 搜索异常、搜索为空和详情失败产生不同状态。
- 每个标题路径都保存查询词、季号、返回数量和错误。
- 任一路径成功时不被其他路径失败覆盖。

### 5.4 “篇”匹配

- `鬼灭之刃 无限列车篇` 能生成完整标题和篇章两条路径。
- 能通过 TMDB 季名选中正确的 `season_number`。
- 豆瓣 7 集与 TMDB 季 7 集形成辅助证据。
- 没有清晰分隔符的原生“篇”标题不被强制拆分。
- TMDB 季名只有 `Season 2` 时不能仅凭 7 集自动接受。
- 同时存在两个相近篇章季名时进入 `ambiguous`，不能自动订阅。

## 6. 推荐实现顺序

1. 增加 MoviePilot 电视剧订阅历史查重。
2. 增加搜索诊断字段和细分失败状态。
3. 实现单条重试 API，并先用静态按钮验证后端流程。
4. 实现带输入框、失败筛选、分页和行内重试的自定义页面。
5. 实现 `arc_title` 假设和 TMDB 分季名称匹配。
6. 使用“小丑回魂：欢迎来到德里镇 第一季”和“鬼灭之刃 无限列车篇”做真实宿主回归。

## 7. 不变约束

- 正式订阅总集数继续只使用豆瓣明确总集数。
- TMDB 季集数只作为匹配证据，不能写入正式订阅总集数。
- 自动匹配仍需满足固定最低分和领先分。
- 失败重试不得绕过候选评分和安全阈值。
- 不修改用户已有订阅或订阅历史。
- 每个自动决策必须能够从历史记录中解释使用了哪些搜索词和证据。
